---
title: "Agents"
description: "What an agent is made of: entries, sub-agents, context groups, and the path a run takes between them"
icon: "robot"
---

An agent is the unit you build, store, and run in ServFlow. It holds how a run starts, which AI models are called, in what order, and with what tools. This page explains the parts and how a run moves through them. To build one, see [Create an agent](/guides/create-an-agent).

## What an agent is made of

| Part | What it is |
|---|---|
| **Entries** | How the agent is started: by a service such as Discord, by your own application through a webhook, by a schedule, or by you. An agent has at least one. See [Entries and entry handlers](/concepts/entries). |
| **Sub-agents** | The model calls. Each has a provider, a system prompt, a user prompt, and its own tools. |
| **Context groups** | Steps that run before a sub-agent to gather what its prompt needs, such as looking something up in a database or another service. |

An agent also has a name, an optional description, and an id.

## How a run moves

An entry names where a run starts: a sub-agent, or a context group that leads to one. From there, every sub-agent and context group names what comes next. That is another sub-agent, another context group, or the end of the run.

The dashboard's starting arrangements are patterns of these links. A sequential pipeline chains sub-agents, and each one receives the reply of the one before it. A supervisor routes work to specialists by calling them as tools. A parallel fan-out hands work to several workers and collects their replies in an aggregator.

A run has no separate response step. Its answer is what its sub-agents said, and the entry decides who receives it.

## Sub-agents

A sub-agent is one model with one job. Its **provider** is a stored connection to an AI service, and the model belongs to the provider. Two sub-agents that need different models use two providers.

The **system prompt** sets the sub-agent's role. The **user prompt** is a template filled in on every run from what the entry delivered, such as `{{ body "question" }}` for a webhook or `{{ .discord.content }}` for a Discord message.

A sub-agent can call [tools](/guides/add-a-tool) while it works: an action, tools from an outside service, another sub-agent, or another agent. It can also have a **workspace**, a folder of files it reads and writes with built-in file tools. A workspace is how a sub-agent keeps notes from one run to the next.

## Context groups

A context group is a set of steps that run before a sub-agent, every time, to gather what its prompt needs. The dashboard calls this context gathering. See [Context fetching](/concepts/context-fetching) for how it works and when to use a step instead of a tool.

## Identity and state

Every agent has an id made from its name, such as `question-answerer`. Anything that refers to the agent uses that id, including a tool on another agent that hands it a task.

An agent is either enabled or disabled. A disabled agent stays stored, but nothing can start it until you enable it again.

## Related

<CardGroup cols={2}>

<Card title="Create an agent" icon="robot" href="/guides/create-an-agent">
  Build an agent in the dashboard or from a config file, and call it from outside.
</Card>

<Card title="Entries and entry handlers" icon="right-to-bracket" href="/concepts/entries">
  How a run starts, and where its reply goes.
</Card>

<Card title="Tools" icon="wrench" href="/references/tools">
  Everything a sub-agent can call during a run.
</Card>

<Card title="Actions" icon="play" href="/concepts/actions/overview">
  What context steps and action tools run.
</Card>

</CardGroup>
